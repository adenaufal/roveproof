---
status: active
submissionDeadline: 2026-07-22T07:00:00+07:00
internalFreeze: 2026-07-21T12:00:00+07:00
amendedAt: 2026-07-18
modelBackend: ChatGPT subscription through Codex CLI; no API billing
---

# Roveproof battle plan

Approximately 102 hours remain from bootstrap to the researched submission deadline. Optimize for one undeniable proof loop, not platform breadth.

## 18 July — foundation and baseline

- Freeze Roveproof working name, PRD, architecture, contracts, and design direction.
- Scaffold workspaces and both web apps.
- Build seeded checkout and synthetic order endpoint.
- Implement Indonesia profile and constrained runner.
- End the day with a real baseline evidence bundle and the three visible defects.

**Cut line:** no model integration before deterministic baseline proof exists.

## 19 July — control UI and analysis

- Build single-run worker/store/SSE.
- Build dashboard against golden and real baseline evidence.
- Add subscription-authenticated Codex CLI structured evidence analysis and citations.
- Tune target payload/profile so 8.2 MB / 19 s is measured and explainable.

**Cut line:** no extra dashboard pages, settings, account system, or profile editor.

## 20 July — verified repair loop

- Add two read-only subscription-backed Codex diff-authoring calls: test-only, then source-only.
- Start Docker engine and enforce the candidate sandbox.
- Prove baseline failing test, bounded candidate, clean verifier, and exact diff hash.
- Tune repaired target to measured 1.4 MB / 6 s without hard-coded result substitution.
- Add hash-bound human approval.

**Cut line:** one candidate attempt only; no auto-merge/deploy.

## 21 July — hardening and submission

- Morning: independent review/fix loop, security redaction tests, full build/typecheck/test/E2E.
- By **12:00 WIB**: feature and copy freeze.
- Afternoon: three clean demo rehearsals, record 1280×720 walkthrough, produce screenshots and architecture diagram.
- Evening: final README, Devpost copy, repository/license cleanup, submission upload.
- Preserve an overnight buffer before **22 July 07:00 WIB** deadline.

## Three-minute judging narrative

- **0:00–0:25 — Problem:** “Your CI passes because it tests your laptop, not your users.”
- **0:25–0:55 — Baseline:** run Indonesia Mobile; show checkout fail, 8.2 MB, 19 s, evidence.
- **0:55–1:25 — Intelligence:** subscription-backed Codex cites the three causal failures from real artifacts with schema-valid provenance.
- **1:25–2:10 — Safe repair:** Codex returns a failing-test diff and bounded source diff; Roveproof applies and executes them only inside the sandbox and shows diff policy.
- **2:10–2:40 — Independent proof:** clean rerun succeeds at 1.4 MB / 6 s.
- **2:40–3:00 — Trust and vision:** human approves exact hash; “Built in Indonesia to test software for the realities of the world.”

## Daily kill criteria

- Baseline not deterministic by end 18 July → simplify fixture, never fake evidence.
- ChatGPT-authenticated Codex CLI unavailable, logged out, quota-blocked, or schema-incompatible by midday 19 July → resolve subscription access/CLI compatibility; fixture remains rehearsal-only and never fall back to API billing.
- Docker unavailable by morning 20 July → fix engine/WSL; do not execute generated code on host.
- Live patch loop unstable by evening 20 July → freeze one prevalidated candidate path while still running real test/sandbox/verifier steps.
- Demo exceeds three minutes → cut explanation, never cut independent verification or human approval.

## Active skill limit

Maximum three per phase:

- **Naming/design direction:** `product-name` + existing `dyor-research` + existing `brandkit`.
- **UI implementation:** `interface-design` + `vercel-react-best-practices` + `world-readiness-qa`.
- **Polish/performance:** `better-ui` + `core-web-vitals` + existing `playwright-best-practices`.
- **Video:** `ui-demo` + existing `playwright-best-practices` only.
- **Repair loop:** `verified-codex-patch-loop` + `world-readiness-qa` only.
